# Decision Log: Worker orchestration + plugin marketplace

Append-only. Backfilled on 2026-07-14 from repository history, especially commits
`626406b`, `89e4935`, `630774a`, and `b0ad6f6`. Entries describe intent inferred
from commit messages, `ROADMAP.md`, and the shipped scripts; ownership and any
item marked `(confirm)` still require maintainer validation. This log is the
authoritative record for the foundational decisions that the DELEGATE-CODER-001
pack references.

| # | Date | Decision | Context / why | Alternatives considered | Consequence | Owner |
|---|---|---|---|---|---|---|
| 1 | 2026-06-13 | **Backfilled:** split roles — Claude orchestrates/reviews, a cheaper CLI agent executes | `626406b` (v0.2.0) shipped the skill, plugin, and benchmark on the premise that judgment tokens are expensive and execution tokens can be cheap | Do everything in Claude; use a single agent | Token spend concentrates on decisions; execution moves to a worker | Maintainer *(confirm)* |
| 2 | 2026-06-13 | **Backfilled:** two task modes — `read` (read-only, full trust) and `exec` (change-making, always verified) | `SKILL.md` and `delegate.sh` encode read-only worker invocations for analysis and verified invocations for edits | One "do it" mode; trust worker summaries | Read work is zero-risk; edits are gated by git diff + tests | Plugin owner *(confirm)* |
| 3 | 2026-06-15 | **Backfilled:** package as the `tan-tools` marketplace with a nested plugin and a stable core skill | `89e4935` and `ROADMAP.md` established the marketplace/plugin layout and a public install path | Flat standalone skill; publish only scripts | Plugin manifests and slash commands become the canonical integration points | Maintainer *(confirm)* |
| 4 | 2026-06-15 | **Backfilled:** keep the benchmarked default path byte-identical; make all new control opt-in | `ROADMAP.md` "golden rule": only `SKILL.md`, `detect.sh`, and the benchmarked `delegate.sh` invocation force a re-benchmark | Rerun v1 on every change; change defaults immediately | New behavior needs new evidence; `benchmark/RESULTS.md` stays historical truth | Benchmark owner *(confirm)* |
| 5 | 2026-06-14 | **Backfilled:** freeze the v1 benchmark dataset; forbid v2 reruns over it | An accidental v2 re-run was stopped and its partial `results/` wiped (`ROADMAP.md` note) | Overwrite with the latest numbers | v1 remains the published result; new configs publish as new columns | Benchmark owner *(confirm)* |
| 6 | 2026-06-17 | **Backfilled:** add operational control around the stable adapter — model selection, audit log + `stats`, scope/enable switches, `fallback` policy, `allow_paths`, test-command autodetect | `630774a` (v0.2.1) fixed the Codex stdin deadlock and log robustness; `b0ad6f6` (v0.2.2) added model selection, autodetect, and the provider privacy warning | Trust worker summaries; unrestricted execution; no telemetry | Delegation becomes measurable and constrainable without changing the core trust model | Plugin owner *(confirm)* |
| 7 | 2026-06-17 | **Backfilled:** warn when a worker sends code off-machine; exempt purely local workers | `references/setup.md` and `/delegate-setup` show a one-time privacy warning for hosted providers | Silent delegation; block hosted workers outright | Users make an informed privacy choice; local workers stay frictionless | Plugin owner *(confirm)* |
| 8 | 2026-07-14 | **Note:** documentation of the contract router is tracked in DELEGATE-CODER-001, layered on this default path | Keeps the new strict single-file execution mode isolated from the benchmarked `read`/`exec` orchestration | Fold the router into this pack | Clear separation between foundational orchestration and the opt-in router | Maintainer *(confirm)* |
| 9 | 2026-07-14 | **Correction (inferred/backfilled):** `read` is zero-write-risk only when the active adapter enforces a read-only or dry-run control | Corrects prior backfilled entry 2 after reviewing current adapter behavior: Gemini, Qwen, and OpenCode `read` invocations do not enforce a zero-write guarantee | Treat every `read` adapter as full trust; remove non-enforcing adapters | Orchestrators must review and isolate Gemini/Qwen/OpenCode `read` work; only enforcing adapters receive the zero-write-risk designation | Plugin owner *(confirm)* |

# Decision Log: Contract-driven local Qwen worker

Append-only. This log was backfilled on 2026-07-14 from the repository history,
especially commits `626406b`, `89e4935`, `630774a`, `b0ad6f6`, `f88aa20`,
`0c70396`, and `b620adb`.
The entries describe intent inferred from commit messages and surrounding docs;
ownership and any policy marked `(confirm)` still require maintainer validation.

| # | Date | Decision | Context / why | Alternatives considered | Consequence | Owner |
|---|---|---|---|---|---|---|
| 1 | 2026-06-15 | **Backfilled:** package the project as the `tan-tools` Claude Code plugin marketplace | `626406b` established the marketplace/plugin layout and public install path | Keep a flat standalone skill; publish only scripts | Plugin manifests and install guidance become canonical integration points | Maintainer *(confirm)* |
| 2 | 2026-06-17 | **Backfilled:** keep the v1 benchmark dataset frozen and make v2 controls opt-in | `89e4935` and `ROADMAP.md` prioritize byte-identical default behavior and honest comparison | Rerun v1 with every enhancement; change defaults immediately | New behavior needs separate evidence; `benchmark/RESULTS.md` remains historical truth | Benchmark owner *(confirm)* |
| 3 | 2026-06-17 | **Backfilled:** add audit logging, stats, model selection, scope/fallback controls, and path allowlisting around the stable adapter | `630774a` and `b0ad6f6` added operational control and reproducibility features | Depend only on worker summaries; use unrestricted execution by default | Delegation can be measured and constrained without changing the core trust model | Plugin owner *(confirm)* |
| 4 | 2026-07-14 | **Backfilled:** add an opt-in contract mode using raw Ollama generation and full-file replacement | `f88aa20` adds `contract-router.sh`, dispatcher support, and focused tests to reduce chat-loop/prefill overhead | Continue multi-turn chat mode; perform targeted search/replace; trust summaries | A cloud orchestrator can send a low-token, single-file execution unit with deterministic output | Maintainer *(confirm)* |
| 5 | 2026-07-14 | **Backfilled:** make safety gates part of the router contract | `f88aa20` tests path guards, atomic writes, truncation rejection, bounded timeouts, one retry, and `NOOP` | Write any model output; retry indefinitely; allow unbounded commands | Incorrect or incomplete worker output is blocked or made visible before handoff | Plugin owner *(confirm)* |
| 6 | 2026-07-14 | **Proposed:** keep contract mode separate from the benchmarked `read`/`exec` path | The new mode is explicitly invoked as `delegate.sh contract`; existing benchmark policy forbids accidental dataset mutation | Replace the existing adapter path with the local model | Easier rollout and clearer benchmark accounting; new mode still needs its own measurements | Maintainer / benchmark owner *(confirm)* |
| 7 | 2026-07-14 | **Backfilled:** preflight the complete prompt before Ollama eviction or HTTP generation | `b620adb` added an estimated prompt-size guard, including correction context, after the risk of silent input truncation was identified | Rely only on output `done_reason`; let Ollama truncate the input | Oversized contracts fail without changing the target or disturbing resident models | Plugin owner *(confirm)* |
| 8 | 2026-07-14 | **Backfilled:** treat contract privacy as loopback-only by default, not as an unconditional local guarantee | `b620adb` documented that `OLLAMA_HOST` can be overridden and then receives prompt/file contents | Claim all contract runs are local; forbid host overrides | Users get a local default with an explicit remote-endpoint disclosure | Maintainer *(confirm)* |
| 9 | 2026-07-14 | **Backfilled:** reject Python-less contract batches and audit the report's top-level aggregate status | `0c70396` hardened array parsing and changed dispatcher status extraction to read the report status line rather than grep child text | Let arrays fall through to the single-contract regex fallback; infer status by substring order | Batch failures are explicit and mixed `PASS`/`NOOP` batches are recorded as `PASS` | Plugin owner *(confirm)* |
| 10 | 2026-07-14 | **Implemented:** make contract execution transactional and require a clean isolated Git branch | Merge-blocking review identified that writing before verification could leave a failed candidate in the worktree | Direct replacement; reset only after failure; allow dirty targets | Snapshot bytes/mode/existence, stage for tests, promote only after success, and restore every unsuccessful exit | Plugin owner *(confirm)* |
| 11 | 2026-07-14 | **Implemented:** replace fence extraction with strict Ollama structured output | Markdown and source files can contain nested or triple-backtick fences, making first-fence extraction ambiguous | Parse the first code block; trust free-form model text | `{updated_file}` with no additional fields preserves arbitrary UTF-8 and makes malformed/truncated output a hard failure | Plugin owner *(confirm)* |
| 12 | 2026-07-14 | **Implemented:** treat local contract as an explicit backend and benchmark it separately | Heavy orchestrators need an opt-in local implementation path without changing default `read`/`exec` or laundering historical Claude+MiMo evidence into Qwen claims | Change the default backend; silently fall back to hosted agents; overwrite v1 results | `implementation_backend: agent` remains default, contract failures are visible, and fresh five-warm local measurements use additive files only | Maintainer / benchmark owner *(confirm)* |
| 13 | 2026-07-15 | **Implemented:** make child execution and benchmark evidence transactionally attributable | PR #1 review required pre-branch validation, outside-target rollback, consumer-log isolation, deterministic local-runner evidence, and JSONL reproduction of frozen v1 | Create a branch before validation; restore only the target; track runtime logs in consumer files; infer benchmark aggregates from one-run fixtures | Validation occurs before branch creation, each child restores its full eligible worktree snapshot on failure, `.git/info/exclude` protects the audit log, the local runner has a no-live-model test, and `report.py` reproduces 24 A plus 24 B rows without changing frozen files; local-Qwen speed remains unproven | Plugin / benchmark owner *(confirm)* |
| 14 | 2026-07-15 | **Implemented:** complete preflight before branch creation for batches and project configuration | Follow-up review reproduced a later batch path and `contract.num_ctx: 0` creating a delegate branch before failure | Validate only the top-level JSON shape; resolve settings after branch creation | Direct batches validate every child target before switching, and `delegate.sh` validates all paths plus configured positive numeric limits before switching; both regressions are deterministic tests | Plugin owner *(confirm)* |
| 15 | 2026-07-15 | **Implemented:** constrain rollback to Git-visible state and restore the index | Final safety review found broad `.claude/` exclusion, prompt checks after branch/GPU mutation, staged-file leakage on failure, and ignored dependency trees copied into snapshots | Ignore the whole runtime directory; snapshot after branch creation; restore only filesystem bytes; enumerate all ignored files | Only the audit log is excluded, prompt budgets are checked before branch/eviction, tracked/nonignored files and index entries restore transactionally, ignored dependency/cache/build trees are excluded, and trusted test commands must not mutate refs or commit | Plugin owner *(confirm)* |

---
name: delegate doctor
description: Runs a health check to verify worker agents are installed and authenticated.
---

# Run Delegate Coder Doctor

Run the doctor script and display its output to the user:
```bash
bash ${CLAUDE_PLUGIN_ROOT}/skills/delegate-coder/scripts/doctor.sh --all
```

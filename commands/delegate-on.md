---
name: delegate on
description: Enables the delegate-coder skill for this project.
---

# Enable Delegate Coder

Set `"enabled": true` in `.claude/delegate-coder.json` using the `jq` command line tool or `python3`. If the file doesn't exist, create it with `{"enabled": true}`. Inform the user that the skill is now enabled.
